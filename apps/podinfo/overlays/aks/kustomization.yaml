apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - ingress.yaml

# Publica en https://podinfo.azure.jalcalaroot.com via el Application
# Gateway compartido (AGIC).
replicas:
  - name: podinfo
    count: 2

namespace: default

patches:
  - path: patch-virtual-node.yaml
