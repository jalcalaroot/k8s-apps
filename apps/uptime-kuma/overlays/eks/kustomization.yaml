apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# Sin Ingress por ahora - mismo motivo que podinfo (ver README/CLAUDE.md).
# Probar con: kubectl port-forward svc/uptime-kuma 3001:3001
namespace: default
