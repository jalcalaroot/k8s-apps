apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - ingress.yaml

# Publica en https://uptime-kuma.azure.jalcalaroot.com via el Application
# Gateway compartido (AGIC).
namespace: default

patches:
  - path: patch-virtual-node.yaml
  - path: patch-slow-start.yaml
