apiVersion: v1
kind: Service
metadata:
  name: "game-2048"
  annotations:
    checkov.io/skip1: "CKV_K8S_21=mismo motivo que deployment.yaml - namespace default a proposito"
spec:
  selector:
    app: "game-2048"
  ports:
    - name: http
      port: 80
      targetPort: http
