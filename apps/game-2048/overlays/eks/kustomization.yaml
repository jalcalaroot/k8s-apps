apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - ingress.yaml

# Publica en https://game-2048.aws.jalcalaroot.com via el ALB compartido.
replicas:
  - name: "game-2048"
    count: 2

namespace: default
