apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# Sin Ingress por ahora - mismo motivo que podinfo (ver README/CLAUDE.md).
# Probar con: kubectl port-forward svc/game-2048 8080:80
replicas:
  - name: "game-2048"
    count: 2

namespace: default
