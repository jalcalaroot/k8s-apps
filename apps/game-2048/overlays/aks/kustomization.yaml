apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - ingress.yaml

# Publica en https://game-2048.azure.jalcalaroot.com via el Application
# Gateway compartido (AGIC).
replicas:
  - name: "game-2048"
    count: 2

namespace: default

patches:
  - path: patch-virtual-node.yaml
